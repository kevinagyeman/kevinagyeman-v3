---
import Footer from '@/components/astro/Footer.astro';
import Navbar from '@/components/Navbar';
import '@/styles/global.css';
import type { Seo } from '@/types/seo-type';
import { SEO } from 'astro-seo';

const props = Astro.props as { content: Seo };
const { content } = props;
---

<script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
      return localStorage.getItem('theme');
    }
    return window.matchMedia('(prefers-color-scheme: dark)').matches
      ? 'dark'
      : 'light';
  };
  const isDark = getThemePreference() === 'dark';
  document.documentElement.classList[isDark ? 'add' : 'remove']('dark');

  if (typeof localStorage !== 'undefined') {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains('dark');
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ['class'],
    });
  }
</script>

<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width' />
    <link rel='sitemap' href='/sitemap-index.xml' />
    <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
    <SEO
      title={content.title}
      description={content.description}
      openGraph={{
        basic: {
          title: content.title || 'Kevin Agyeman',
          type: 'website',
          image: content.image || 'https://www.kevinagyeman.com/cover.png',
        },
      }}
      twitter={{
        creator: '@kevinagyeman_',
      }}
      extend={{
        link: [{ rel: 'icon', href: '/favicon.ico' }],
        meta: [
          {
            name: 'twitter:image',
            content: 'https://www.kevinagyeman.com/cover.png',
          },
          { name: 'twitter:title', content: content.title },
          { name: 'twitter:description', content: content.description },
        ],
      }}
    />
  </head>
  <body>
    <div class='flex min-h-screen flex-col'>
      <Navbar client:visible />
      <div class='container flex-grow mx-auto px-4 sm:px-0'>
        <slot />
      </div>
      <Footer />
    </div>
  </body>
</html>
