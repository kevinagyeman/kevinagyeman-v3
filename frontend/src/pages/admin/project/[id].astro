---
import ProjectForm from '@/components/ProjectForm';
import MainLayout from '@/layouts/main.astro';

const { id } = Astro.params;
---

<MainLayout content={{ title: 'Edit Project' }}>
  <ProjectForm projectId={id} client:visible />
</MainLayout>
